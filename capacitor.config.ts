import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front e-gouvernance',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
